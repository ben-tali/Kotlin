package io.baraka

fun main() {
    val jay = Student()
    jay.printInfo()
}

/*
access modifiers
in kotlin everything (visibility) in general is public by default
public
internal (public within the module)
private (available within the file of implementation)
protected (available within the class/subclass)
 */